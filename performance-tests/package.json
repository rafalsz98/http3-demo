{
  "name": "performance-tests",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start": "bun run index.ts",
    "docker:build": "docker build . -t perf",
    "docker:run": "docker run -v ./data:/app/data --rm perf"
  },
  "dependencies": {
    "commander": "^11.1.0"
  }
}
